using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum BT_Action
{
    DoNothing,
    DoAttack,
    DoMoveTowardsPlayer,
    DoJump
}
public class BT_Behaviors : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
